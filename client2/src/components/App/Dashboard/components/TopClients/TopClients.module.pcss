.container {
    display: flex;
    flex-flow: column;
    background-color: var(--white);
}

.title {
    font-size: 14px;
    line-height: 22px;
    margin-bottom: 4px;
    padding: 24px;
    color: var(--gray300);
}

.content {
    font-size: 30px;
    line-height: 38px;
    margin-bottom: 18px;
    color: var(--gray900);
}